# Skip optional dependencies that might cause build failures
ignore-optional=true
# Avoid warnings about optional dependencies
loglevel=error
# Ensure we don't try to build native modules if they fail
unsafe-perm=true 